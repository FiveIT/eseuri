table:
  name: work_status
  schema: public
is_enum: true
array_relationships:
- name: works
  using:
    foreign_key_constraint_on:
      column: status
      table:
        name: works
        schema: public
